# מסמך פריסה למחלקת IT — מערכת ניתוח הסטות תנועה לאוטובוסים

## תיאור כללי
אפליקציית web יחידה (single HTML file) שמיועדת לסגן הנשיא לתכנון תנועה. תפקידה לקבל מסמכים על שיבושי תנועה (אימייל / Word / PowerPoint), לזהות קווי אוטובוס מושפעים, להמליץ על מסלולי הסטה, ליצור הודעת תנועה ב-Word, ולשלוח לאישור מנהל לפני הפצה.

**הקובץ:** `traffic-diversion.html`
**סוג:** static HTML, ללא backend, ללא database, ללא תהליכי build.

---

## דרישות סביבה

### הפעלה
- דפדפן מודרני: Chrome 100+ / Edge 100+ / Firefox 100+ / Safari 15+
- חיבור אינטרנט יוצא לכתובות הבאות (להוסיף ל-allow-list בחומת אש):

| יעד | פרוטוקול | מטרה | רגישות |
|---|---|---|---|
| `api.anthropic.com` | HTTPS | קריאות LLM (Claude API) | נשלח טקסט המסמך |
| `open-bus-stride-api.hasadna.org.il` | HTTPS | נתוני GTFS — תחנות וקווים (ישראל) | רק קואורדינטות |
| `nominatim.openstreetmap.org` | HTTPS | Geocoding (כתובת → קואורדינטות) | רק כתובת |
| `tile.openstreetmap.org` | HTTPS | Map tiles | אין נתונים |
| `unpkg.com`, `cdnjs.cloudflare.com` | HTTPS | טעינת ספריות צד-שלישי (CDN) | אין נתונים |

> **המלצה:** את ספריות ה-CDN ניתן להוריד ולארח פנימית כדי להסיר תלות ב-CDN חיצוני. אנו ממליצים על כך.

### פריסה
**אופציה A — שיתוף קובץ פנימי (הכי פשוט):**
שמירת הקובץ על שרת קבצים פנימי / SharePoint, פתיחה דרך דפדפן.

**אופציה B — Web server פנימי (מומלץ):**
פריסה ב-IIS / nginx פנימי. דרוש: שרת web סטטי בלבד. ללא PHP/Node/Python.
```
# nginx
server {
    listen 443 ssl;
    server_name traffic-tools.mta.internal;
    root /var/www/traffic-diversion;
    index traffic-diversion.html;
}
```

**אופציה C — Intranet portal:**
הטמעה כקובץ סטטי בתוך פורטל ארגוני קיים.

---

## אבטחה — נושאים לטיפול

### 1. ניהול מפתחות API
**בעיה:** כיום מפתח Claude API נשמר ב-localStorage של דפדפן המשתמש. זה מתאים ל-PoC אך לא לסביבה ארגונית.

**פתרון מומלץ לארגון:** שילוב reverse-proxy פנימי המחזיק את המפתח, והאפליקציה תפנה אליו במקום ל-`api.anthropic.com` ישירות.
- דוגמה ל-nginx proxy שמסתיר את המפתח:
```nginx
location /api/claude/ {
    proxy_pass https://api.anthropic.com/;
    proxy_set_header x-api-key $CLAUDE_KEY;
    proxy_set_header anthropic-version "2023-06-01";
}
```
לאחר שינוי כזה, יש לעדכן ב-`traffic-diversion.html` את ה-URL מ-`https://api.anthropic.com/v1/messages` ל-`/api/claude/v1/messages` ולהסיר את שדה ה-API key מההגדרות.

### 2. מידע רגיש בטקסטים
המסמכים שמועלים נשלחים לעיבוד ל-**Anthropic** (Claude API). יש לוודא:
- חתימת DPA (Data Processing Agreement) מול Anthropic
- אין במסמכים שמות עובדים / מידע אישי שהוא PII רגיש
- שקילת שימוש ב-Anthropic via AWS Bedrock / Google Vertex AI (אם קיים חוזה ארגוני)

### 3. שליחת אימייל
האפליקציה משתמשת ב-`mailto:` — נפתחת טיוטה ב-Outlook של המשתמש, שליחה ידנית. **אין שליחה אוטומטית של האפליקציה.**

זה תכן בכוונה כדי:
- להעניק שליטה אנושית לפני שליחה
- לא לדרוש credentials של SMTP / Graph API
- להשתמש ב-Outlook הקיים (חתימה, רשימות תפוצה, היסטוריה)

### 4. נתונים מקומיים בדפדפן
ב-localStorage נשמרים: מפתח API, שם שולח, נספח קווים. אין מידע אישי של נוסעים. ניתן לנקות דרך הגדרות הדפדפן.

---

## תחזוקה שוטפת

### עדכון ספריות צד-שלישי
הקובץ משתמש בגרסאות מקובעות מ-CDN. אין צורך בעדכון אלא אם נדרש לאבטחה.

### עדכון מודל Claude
בקובץ HTML, חיפוש `claude-sonnet-4-6` והחלפה לדגם חדש בעת יציאתו. גם תפריט הבחירה בהגדרות.

### עדכון נספח קווים
המשתמש מעלה קובץ Excel ידנית (פורמט עם עמודות: `קו`, `תיאור`, `רחובות`, `מפעיל`). אין DB מרכזי — פשוט קובץ Excel מנוהל ע"י תכנון תנועה.

### לוגים
האפליקציה לא כותבת לוגים בעצמה. אם נדרש audit, מומלץ:
- שימוש בלוגים של ה-reverse-proxy (אופציה 1 לעיל)
- הוספת קוד שולח אירועים ל-SIEM פנימי

---

## בדיקות לקבלה (UAT)

לפני העברה לפרודקשן, מומלץ לבדוק:
- [ ] טעינת מסמך Word עם פרטי שיבוש אמיתי (ראה דוגמה מובנית בקובץ)
- [ ] חילוץ פרטים נכון (Claude API)
- [ ] זיהוי קווים מושפעים — האם תואם למה שהמתכנן יודע?
- [ ] איכות הצעת ההסטה — האם הגיונית עבור אוטובוס?
- [ ] הורדת קובץ Word — האם הפורמט תקין?
- [ ] mailto נפתח ב-Outlook — האם הטיוטה מלאה?
- [ ] קישור אישור — האם פותח את האפליקציה במצב אישור ומאפשר טיוטה לתפוצה?

## מי לפנות
שאלות על הקוד / שינויים פונקציונליים — לסגן הנשיא לתכנון תנועה, שיכוון לצוות הפיתוח שבנה את הקובץ.

---

**גרסה:** 1.0 (Prototype)
**מאגר git:** `zagronuri-lgtm/efficiency-dashboard`
**ענף:** `claude/parse-traffic-diversion-emails-VTWd4`
**קובץ:** `traffic-diversion.html`
